# railway.toml — place this in your backend/ folder
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn main_v2:app --host 0.0.0.0 --port $PORT --workers 2"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
